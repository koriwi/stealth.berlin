---
import { iff } from "../lib";

interface Props {
  text: string;
}
const { text } = Astro.props;
const noUnderscore = iff(text.startsWith("_"), text.slice(1), text);
const cleanText = noUnderscore[0].toUpperCase() + noUnderscore.slice(1);
---

{cleanText}
